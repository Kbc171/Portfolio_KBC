# 3D Asset Drop Zone

Place the final real-time character asset here when it is ready.

Expected primary file:

- `chandra_guide_character.glb`

If `gltf` is delivered instead, keep the folder self-contained and preserve texture paths.

Related production documents:

- `/CHARACTER_3D_BRIEF.md`
- `/character-asset-spec.json`
